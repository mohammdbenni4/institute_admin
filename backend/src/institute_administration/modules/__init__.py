"""Bounded contexts (DDD modules).

Each subpackage here is a self-contained bounded context with its own domain,
application, infrastructure and presentation layers. The project starts with no
modules — see ``README.md`` for the recommended layout when you add one.
"""
