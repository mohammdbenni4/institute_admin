# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local env files (each subproject keeps its own .env)
.env
.env.*
!.env.example

# Local Claude Code settings
.claude/settings.local.json

# graphify code knowledge graph — regenerated for free with `graphify update .`
graphify-out/
