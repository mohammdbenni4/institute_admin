# Copy to `.env`. Variables prefixed with PUBLIC_ are exposed to the browser.
PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# true = run entirely on in-browser demo data (no backend needed). Login with
# teacher@demo.com / 123456.
PUBLIC_MOCK_MODE=false
